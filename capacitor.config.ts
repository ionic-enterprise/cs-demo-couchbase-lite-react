import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cs-demo-couchbase-lite-react',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
